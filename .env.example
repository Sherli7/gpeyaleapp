# ===========================================
# CONFIGURATION SERVEUR
# ===========================================
PORT=3003
NODE_ENV=development

# ===========================================
# CONFIGURATION BASE DE DONNÉES POSTGRESQL
# ===========================================
# ⚠️  TOUTES CES VARIABLES SONT OBLIGATOIRES
# L'application refusera de démarrer si l'une d'elles est absente

# Hôte PostgreSQL
# - En local: localhost
# - Avec Docker: host.docker.internal (Windows/Mac) ou l'IP de l'hôte (Linux)
DB_HOST=localhost

# Port PostgreSQL (standard: 5432)
DB_PORT=5432

# Utilisateur PostgreSQL
# ⚠️  IMPORTANT: Utilisez un utilisateur avec les permissions nécessaires
# Ne pas utiliser 'postgres' en production
DB_USER=votre_utilisateur

# Mot de passe de l'utilisateur PostgreSQL
DB_PASSWORD=votre_mot_de_passe

# Nom de la base de données
DB_NAME=votre_base_de_donnees

# ===========================================
# CONFIGURATION EMAIL (SMTP)
# ===========================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=votre-email@example.com
EMAIL_PASS=votre-mot-de-passe-application
EMAIL_FROM=noreply@example.com

# ===========================================
# CONFIGURATION CORS
# ===========================================
FRONTEND_URL=http://localhost:4200

# ===========================================
# RATE LIMITING
# ===========================================
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX=100            # Nombre maximum de requêtes par fenêtre
